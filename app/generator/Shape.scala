package generator

import breeze.linalg._
import generatorjava.spherical.Point

object Shape {
  def name = "hello"

//  def render(p : java.util.ArrayList[Point]): Unit = {
//    var p2 : List[Point] = List()
//    var i = 0
//    for( i <- 0 until p.size() ) p2 = p2+:p;
//
//  }
}
